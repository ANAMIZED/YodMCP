# Contributing

1. Fork and branch from `main`.
2. `pip install -e ".[dev]"`
3. Add tests under `tests/`.
4. `pytest tests/ -v` and `python scripts/verify_e2e.py`.
5. Open a PR against `main`.

See `docs/ARCHITECTURE.md` and `AGENTS.md`.
